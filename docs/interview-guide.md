# 气象负反馈分析 Agent - 面试指南

> 针对该项目的面试问题及回答整理

---

## 一、项目概述

### 1.1 一句话介绍

> 基于 RAG + Agent 架构的气象负反馈自动分析系统，用户反馈进来后，自动查询气象数据、检索历史案例、调用 LLM 生成分析报告和回复内容。

### 1.2 业务价值

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 排查时间 | 2小时/个 | 5分钟/个 |
| 人力成本 | 100人天/月 | 10人天/月 |
| 响应速度 | 2-3天 | 实时 |

### 1.3 技术栈

```
Python 3.11 + FastAPI + 豆包大模型(Ark) + Milvus + MySQL + BGE-large-zh-v1.5
```

---

## 二、架构设计

### 2.1 整体架构

```
用户反馈 → API 层 → Agent 层 → Skill 层 → 数据层
                         ↓
                    LLM 分析 → 返回结果
```

**分层说明**：
- **API 层**：FastAPI 路由，接收反馈请求
- **Agent 层**：编排 Skill 调用，组装数据给 LLM
- **Skill 层**：独立功能模块（数据查询、知识检索、体感计算）
- **数据层**：MySQL（气象数据）、Milvus（知识库向量）

### 2.2 为什么选 Agent + Skill 架构

**面试话术**：

> "我们用的是 Skill + Agent 混合架构。Agent 负责智能决策——理解用户反馈、决定调用哪些 Skill、组合结果生成回复。Skill 负责固定功能——数据查询、链路检查、计算工具。
>
> 这样做的好处是：Skill 可以独立开发测试，Agent 可以灵活编排，后续新增数据源只需要加 Skill，不用改 Agent 逻辑。"

### 2.3 为什么没用 Function Calling

**面试话术**：

> "我们评估后选择了直接编排的方式，而不是 LLM Function Calling。原因是：
> 1. 我们的 Skill 调用顺序是固定的，不需要 LLM 动态决策
> 2. 直接编排延迟更低，一次 LLM 调用就够了
> 3. Function Calling 需要多次 LLM 调用（决策 + 生成），成本翻倍
>
> 如果后续 Skill 种类增多、需要动态选择，再引入 Function Calling。"

---

## 三、RAG 实现

### 3.1 什么是 RAG

**面试话术**：

> "RAG 是 Retrieval Augmented Generation 的缩写，检索增强生成。核心思路是：先从知识库中检索相关文档，拼入 LLM 的 Prompt，让 LLM 基于这些上下文生成回答。
>
> 解决的问题是：LLM 的知识有截止日期，而且不了解我们业务的历史案例。RAG 让 LLM 能参考最新、最相关的知识。"

### 3.2 我们的 RAG 流程

```
用户反馈: "温度比预报高5度"
    ↓
1. Embedding: 文本转向量 [0.12, -0.34, ...]
    ↓
2. Milvus 检索: 找最相似的 Top-3 历史案例
    ↓
3. 拼入 Prompt: "参考知识: {检索结果}"
    ↓
4. LLM 生成: 结合实时数据 + 历史案例，生成回复
```

### 3.3 为什么选 BGE-large-zh-v1.5

**面试话术**：

> "选型考虑了几个维度：
> 1. **中文优化**：BGE 是智源研究院专门为中文训练的，在中文 MTEB 排行榜上排名靠前
> 2. **开源免费**：MIT 协议，不需要付费调 API
> 3. **本地运行**：不依赖外部服务，延迟可控
> 4. **维度合适**：1024 维，精度比 768 维的小模型好
> 5. **生态成熟**：sentence-transformers 直接支持，社区活跃"

### 3.4 向量检索 vs 关键词匹配

| 维度 | 关键词匹配 | 向量检索 |
|------|-----------|---------|
| 原理 | 字符串包含 | 语义相似度 |
| "热死了" → "温度偏高" | ❌ 匹配不上 | ✅ 语义相近 |
| "又淋雨了" → "降雨偏差" | ❌ 匹配不上 | ✅ 语义相近 |
| "温度比预报高" → 精确匹配 | ✅ 能匹配 | ✅ 能匹配 |
| 实现复杂度 | 低 | 中 |
| 依赖 | 无 | Embedding 模型 + 向量数据库 |

**面试话术**：

> "我们实现了两层检索：向量检索优先，关键词匹配兜底。向量检索解决语义匹配问题——用户说'热死了'能匹配到'温度偏高'的案例。关键词匹配作为降级方案，确保向量服务不可用时系统仍然可用。"

### 3.5 知识库数据结构

```csv
module,problem_pattern,problem_desc,root_cause,solution,tags,severity
实况,数据偏差,温度比预报高5-10度,阳光直射体感差异,增加体感温度显示,温度,medium
```

**字段说明**：
- `module`：产品模块（14类）— 结构化过滤
- `problem_pattern`：问题模式（5类）— 分类匹配
- `problem_desc`：问题描述 — 向量检索主字段
- `root_cause`：根因分析 — 向量检索增强 + LLM 参考
- `solution`：解决方案 — 返回给 LLM
- `tags`：语义标签 — 向量检索增强
- `severity`：严重程度 — 排序加权

**Embedding 拼接**：`problem_desc + root_cause + solution + tags`

---

## 四、评估指标

### 4.1 核心指标

| 指标 | 定义 | 公式 | 我们的结果 |
|------|------|------|-----------|
| **准确率 (Precision)** | 检索结果中相关的比例 | 相关∩检索 / 检索 | - |
| **召回率 (Recall)** | 所有相关文档中被检索到的比例 | 相关∩检索 / 相关 | - |
| **Top-K 命中率** | Top-K 结果中包含正确答案的比例 | 命中数 / 总数 | 93.3% |
| **MRR** | 正确答案排名的倒数平均 | 1/rank 的平均 | - |

### 4.2 面试常见问题

**问：召回率和准确率怎么平衡？**

> "看业务场景。客服场景**召回率更重要**——宁可多返回几条相关案例给 LLM 参考，也不能漏掉关键信息。所以我们用 Top-3 而不是 Top-1。
>
> 但如果场景是精确匹配（比如法律条款检索），**准确率更重要**，返回不相关的结果会有误导。"

**问：怎么评估 RAG 效果？**

> "我们分三层评估：
> 1. **离线评估**：准备标注好的测试用例，跑检索看 Top-K 命中率和 MRR
> 2. **在线评估**：用户反馈 👍/👎 收集真实效果
> 3. **Badcase 闭环**：分析错误案例，补充知识库或优化检索策略"

**问：检索效果不好怎么优化？**

> "几个方向：
> 1. **优化 Embedding 模型** — 换更大的模型或领域微调
> 2. **优化知识库数据** — 补充更多案例，细化标签
> 3. **混合检索** — 向量 + BM25 + 结构化过滤，RRF 融合
> 4. **加 Reranker** — 用 Cross-encoder 精排
> 5. **优化查询** — Query 改写、扩展"

### 4.3 我们的评估脚本

```python
# 测试用例
TEST_CASES = [
    {"query": "温度比预报高5度", "expected_module": "实况"},
    {"query": "没收到预警", "expected_module": "天气预警"},
    {"query": "热死了", "expected_module": "实况"},
    ...
]

# 评估逻辑
for case in TEST_CASES:
    results = search_knowledge(case["query"], top_k=3)
    modules = [extract_module(r) for r in results]
    hit = case["expected_module"] in modules
```

---

## 五、技术细节

### 5.1 Embedding 是什么

**面试话术**：

> "Embedding 是把文本变成一组数字（向量）。语义相近的文本，向量距离也近。
>
> 比如'温度比预报高'和'太热了'，虽然字面完全不同，但 Embedding 后向量很接近，就能匹配上。
>
> 我们用的是 BGE-large-zh-v1.5，输出 1024 维向量。"

### 5.2 Milvus 是什么

**面试话术**：

> "Milvus 是专门做向量检索的数据库。传统数据库做精确匹配，Milvus 做相似度匹配。
>
> 我们用的是 Milvus Lite 版本，数据存在本地文件，适合开发和小规模场景。生产环境可以用 Milvus Standalone 或分布式部署。"

### 5.3 余弦相似度

**面试话术**：

> "余弦相似度衡量两个向量的方向差异。值在 -1 到 1 之间，1 表示完全相同，0 表示无关，-1 表示相反。
>
> 我们用余弦相似度衡量用户查询和知识库条目的相似程度，取 Top-3 最相似的。"

### 5.4 Prompt 工程

**面试话术**：

> "我们的 Prompt 分三部分：
> 1. **System Prompt**：定义 Agent 角色、能力、输出格式
> 2. **数据摘要**：实况数据 + 体感分析 + 逐时预报 + 知识库检索结果
> 3. **用户反馈**：原始的反馈内容
>
> LLM 综合这些信息，生成分析报告和回复。关键是把实时数据和历史案例都给到 LLM，让它有足够上下文。"

---

## 六、项目难点 & 解决方案

### 6.1 中文语义匹配难

**问题**：用户说法多样（"热死了"、"太热了"、"温度偏高"），关键词匹配覆盖不了。

**解决**：用 BGE 向量模型做语义检索，语义相近的文本能匹配上。

### 6.2 知识库数据质量

**问题**：原始 CSV 有脏数据（severity 字段混入其他值），tags 粒度不统一。

**解决**：数据清洗（severity 只保留 high/medium/low），tags 细化（"气象知识" → "体感温度"/"气象原理"/"天气系统"）。

### 6.3 检索精度评估

**问题**：怎么知道检索效果好不好？

**解决**：写评估脚本，准备标注测试用例，计算 Top-K 命中率，分析 Badcase。

---

## 七、后续优化方向

| 方向 | 说明 |
|------|------|
| **混合检索** | 向量 + BM25 + 结构化过滤，RRF 融合 |
| **Reranker** | 用 Cross-encoder 精排，提升精度 |
| **Query 改写** | 用户查询优化后再检索 |
| **知识库微调** | 用业务数据微调 Embedding 模型 |
| **Badcase 闭环** | 分析错误案例自动回写知识库 |
| **多模态** | 雷达图、预警截图分析 |

---

## 八、面试 STAR 法则

### Situation（背景）

> 气象 App 每周收到大量用户负反馈（"天气不准"、"没有预警"等），人工排查每个需要 1-2 小时，响应慢、效率低。

### Task（任务）

> 构建智能 Agent 系统，实现负反馈自动分析，将排查时间从 2 小时降到 5 分钟。

### Action（行动）

> 1. 设计 Skill + Agent 架构，模块化开发
> 2. 对接气象数据源（MySQL），实现实况/逐时/逐天查询
> 3. 构建 RAG 知识库（Milvus + BGE），194 条历史案例
> 4. 实现向量检索 + 关键词兜底的混合检索
> 5. 集成豆包大模型，生成分析报告和回复

### Result（结果）

> 1. 排查时间从 2 小时降到 5 分钟
> 2. 检索 Top-1 命中率 93.3%
> 3. 系统已跑通完整流程，支持单条分析

---

## 九、高频面试题速查

| 问题 | 简答 |
|------|------|
| 什么是 RAG？ | 检索增强生成，先检索知识库再让 LLM 生成 |
| 为什么用 RAG？ | LLM 知识有截止日期，不了解业务历史案例 |
| 怎么评估 RAG？ | 离线：Top-K 命中率；在线：用户反馈 |
| 召回率 vs 准确率？ | 客服场景召回率优先，精确匹配场景准确率优先 |
| Embedding 是什么？ | 文本转向量，语义相近的文本向量距离近 |
| 为什么选 BGE？ | 中文优化、开源免费、本地运行、效果好 |
| 向量检索 vs 关键词？ | 向量解决语义匹配，关键词做兜底 |
| 检索效果不好怎么办？ | 换模型、优化数据、混合检索、加 Reranker |
| 为什么不用 Function Calling？ | 调用顺序固定，直接编排更快更省 |
| 知识库怎么维护？ | CSV 编辑 → 重新入库，后续可做增量更新 |

---

## 十、高含金量面试题（详细版）

### 10.1 你们怎么测试 RAG 检索效果？

**回答**：

> "我们分三层测试：
>
> **第一层：离线评估脚本**
> 准备了 15 个标注好的测试用例，每个用例包含用户查询和期望匹配的模块。跑完后计算 Top-1 和 Top-3 命中率。目前结果是 Top-1 命中率 93.3%。
>
> 测试用例覆盖了主要场景：温度偏差、降雨偏差、预警推送、体感温度、空气质量等。也有边界 case，比如用户说法模糊（'热死了'）或跨领域（'又淋雨了'）。
>
> **第二层：Badcase 分析**
> 脚本会自动输出 Badcase——那些检索结果和期望不符的用例。我们逐条分析原因：是知识库缺少相关案例？还是 Embedding 语义理解不够？还是标签分类有问题？
>
> 比如我们发现'又淋雨了'匹配到了'天气预警'而不是'实况'，分析后发现是因为知识库里'降雨偏差'案例的 module 标注不一致，修正后命中率提升了。
>
> **第三层：在线评估**
> 上线后通过用户反馈（👍/👎）收集真实效果。如果用户点了👎，标记为 Badcase，人工复核后回写知识库。"

**追问：15 个用例够吗？**

> "对于 MVP 阶段够了，覆盖了主要场景。后续会扩充到 50-100 个，覆盖更多边界 case 和长尾场景。工业界一般会维护一个'黄金测试集'，每次知识库更新后都跑一遍回归测试。"

---

### 10.2 LLM 生成的内容不准怎么办？怎么防止幻觉？

**回答**：

> "我们从三个层面控制 LLM 输出质量：
>
> **1. Prompt 约束**
> System Prompt 明确要求 LLM 只基于提供的数据回答，不要编造。输出格式是结构化 JSON，强制 LLM 按字段填充。
>
> **2. 数据注入**
> 把实时气象数据和检索到的历史案例都注入 Prompt，给 LLM 足够的上下文。这样 LLM 是'参考数据生成'而不是'凭空想象'。
>
> **3. 后处理校验**
> 解析 LLM 输出时，检查关键字段是否合理。比如温度值应该在合理范围内（-50℃~60℃），如果超出就标记异常。
>
> **4. 人工兜底**
> 严重问题（如预警、灾害相关）的回复需要人工审核后才能发送给用户。"

**追问：如果 LLM 忽略了检索到的知识怎么办？**

> "这种情况确实可能发生。我们的做法是：
> 1. 在 Prompt 中明确指示'请参考以下知识回答'
> 2. 检索结果放在 Prompt 的显眼位置
> 3. 如果多次出现 LLM 忽略知识的情况，说明知识和问题的相关性不够，需要优化检索"

---

### 10.3 你们的系统怎么保证实时性？

**回答**：

> "我们分数据层和检索层两部分：
>
> **数据层实时性**
> 气象数据通过定时任务从北京局接口拉取：实况每10分钟、逐时每小时、逐天每天3次。数据写入 MySQL，查询时直接读最新记录。
>
> **检索层实时性**
> 知识库更新后需要重新入库（CSV → Embedding → Milvus）。目前是全量重建，194条数据几秒钟完成。后续数据量大了可以做增量更新——只处理新增条目，不重建全量。
>
> **LLM 实时性**
> 每次分析都是实时调用 LLM，不是缓存结果。因为用户反馈内容每次都不同，需要 LLM 针对性分析。"

---

### 10.4 如果检索到的知识和实时数据矛盾怎么办？

**回答**：

> "这是个好问题。举个例子：知识库里有个案例说'温度比预报高5度是因为阳光直射'，但当前天气是阴天。
>
> 我们的处理方式是：
>
> **1. 数据优先级**
> 实时气象数据 > 历史案例。LLM 会同时看到两者，但 Prompt 中明确要求'基于当前数据分析'。
>
> **2. LLM 综合判断**
> LLM 看到实时数据是阴天，就不会引用'阳光直射'的案例，而是找其他原因。
>
> **3. 结果校验**
> 如果 LLM 输出的根因和实时数据明显矛盾（比如说是'阳光直射'但实际是阴天），后处理可以捕获并提示异常。
>
> 这也是为什么我们把实时数据和检索结果都给 LLM——让它自己判断哪些参考有价值。"

---

### 10.5 你们系统的核心技术难点是什么？怎么解决的？

**回答**：

> "主要有三个难点：
>
> **难点1：中文语义匹配**
> 用户说法多样，'热死了'、'太热了'、'温度偏高'表达的是同一个问题，但关键词完全不同。
>
> 解决：用 BGE 向量模型做语义检索，把文本转成 1024 维向量，计算余弦相似度。语义相近的文本向量距离近，就能匹配上。
>
> **难点2：知识库数据质量**
> 原始 CSV 有脏数据——severity 字段混入了'跨部门'、'数据源'等非严重程度的值；tags 粒度不统一，'气象知识'太模糊。
>
> 解决：数据清洗（severity 只保留 high/medium/low），tags 细化（'气象知识'拆成'体感温度'、'气象原理'、'天气系统'等具体标签）。
>
> **难点3：检索效果评估**
> 怎么知道检索结果好不好？没有标准答案。
>
> 解决：构建评估脚本 + 标注测试集。我们准备了 15 个测试用例，每个用例有期望的模块匹配。跑完计算 Top-K 命中率，分析 Badcase 优化知识库。"

---

### 10.6 如果让你重新设计，会怎么改进？

**回答**：

> "有几个方向：
>
> **1. 混合检索**
> 现在只用向量检索，可以加 BM25 关键词检索 + 结构化过滤（按 module、tags 过滤），然后用 RRF（Reciprocal Rank Fusion）融合多路结果。工业界标准做法。
>
> **2. Reranker**
> 向量检索召回 Top-20 后，用 Cross-encoder（如 BGE-Reranker）精排，保留 Top-3。比单纯向量检索精度高 10-15%。
>
> **3. Query 改写**
> 用户查询可能太模糊（'热死了'），可以用 LLM 改写成更明确的查询（'温度偏高体感不适'），再检索。
>
> **4. 知识库微调**
> 用业务数据微调 Embedding 模型，让向量更懂我们的领域。比如'闷热'和'高温高湿'在通用模型里可能不够近，微调后能拉近。
>
> **5. 多轮对话**
> 现在是单轮分析，用户追问时没有上下文。可以加对话历史管理，支持多轮交互。"

---

### 10.7 你们怎么处理检索不到结果的情况？

**回答**：

> "我们的系统有兜底机制：
>
> **1. 双层检索**
> 向量检索失败（Milvus 不可用）→ 自动降级到关键词匹配。关键词匹配也没结果 → 返回空列表。
>
> **2. LLM 兜底**
> 即使检索不到历史案例，LLM 仍然可以基于实时气象数据生成分析。Prompt 中检索结果不是必填项，没有就只给实时数据。
>
> **3. 标记未知**
> 如果 LLM 也无法判断根因，会输出'问题类型: 未知'，建议人工介入。不会强行编造一个答案。
>
> **4. 反馈循环**
> 检索不到的查询会被记录下来，定期分析。如果是高频查询但知识库没有对应案例，就补充进去。"

---

### 10.8 你们的系统延迟是多少？怎么优化？

**回答**：

> "当前延迟大约 3-5 秒，分布如下：
>
> | 环节 | 延迟 |
> |------|------|
> | 气象数据查询 | ~100ms |
> | 向量检索 | ~200ms |
> | LLM 调用 | ~2-4s |
> | 其他 | ~100ms |
>
> 瓶颈在 LLM 调用。优化方向：
>
> **1. LLM 优化**
> - 换更快的模型（如豆包轻量版）
> - 减少 Prompt 长度（只给最相关的 Top-2 而不是 Top-3）
> - 流式输出，用户先看到部分结果
>
> **2. 检索优化**
> - 向量索引用 HNSW 替代 IVF_FLAT，检索更快
> - 热点查询缓存
>
> **3. 并行化**
> 气象数据查询和知识检索可以并行执行，而不是串行。"

---

### 10.9 你怎么评估一个 RAG 系统的好坏？

**回答**：

> "评估 RAG 系统需要从检索和生成两个维度：
>
> **检索质量评估**
> - **Precision@K**：Top-K 结果中相关的比例
> - **Recall@K**：所有相关文档中被检索到的比例
> - **MRR**：第一个正确结果排名的倒数
> - **NDCG**：考虑排名位置的评分
>
> **生成质量评估**
> - **Faithfulness**：生成答案是否忠于检索到的上下文，不编造
> - **Answer Relevance**：答案和问题的相关度
> - **Completeness**：答案是否完整覆盖了问题
>
> **端到端评估**
> - **用户满意度**：👍/👎 反馈
> - **任务完成率**：用户问题是否被解决
> - **Badcase 分析**：错误案例的根因分析
>
> 我们目前主要用 Top-K 命中率 + Badcase 分析，后续会引入 RAGAS 框架做自动化评估。"

---

### 10.10 你这个项目最大的收获是什么？

**回答**：

> "三个收获：
>
> **1. RAG 不是万能的**
> 一开始以为加了向量检索就能解决所有问题，实际发现知识库质量、Prompt 工程、数据清洗同样重要。'Garbage in, garbage out'——知识库数据质量差，检索效果就差。
>
> **2. 评估比实现更重要**
> 花了很多时间写检索代码，但没有评估就不知道效果好不好。后来写了评估脚本，才发现有些 case 匹配不上，反过来优化知识库和检索策略。
>
> **3. 工程化思维**
> 从 Demo 到生产，需要考虑很多：异常处理、降级方案、监控告警、数据更新流程。技术选型要权衡——不是最先进最好，而是最适合当前阶段。"

---

### 10.11 LLM 调用超时怎么办？你们怎么处理？

**回答**：

> "LLM 超时是我们遇到的主要工程问题之一。分析下来有几种情况：
>
> **超时原因**
> 1. **网络延迟**：API 服务不稳定，偶尔响应慢
> 2. **Prompt 过长**：输入 token 多，模型推理时间长
> 3. **并发限流**：API 有 QPS 限制，高并发时排队
> 4. **模型负载**：高峰期模型服务负载高
>
> **解决方案**
>
> **1. 超时配置 + 重试**
> ```python
> # 配置超时时间
> client = AsyncOpenAI(timeout=30.0)
> # 指数退避重试
> for attempt in range(3):
>     try:
>         response = await client.chat.completions.create(...)
>         break
>     except TimeoutError:
>         await asyncio.sleep(2 ** attempt)
> ```
>
> **2. 异步处理**
> 单条分析同步调用，批量分析用后台任务异步处理。用户提交后立即返回 batch_id，后台慢慢跑。
>
> **3. 降级方案**
> LLM 不可用时，返回基础分析结果（只基于规则判断），不阻塞用户。
>
> **4. 缓存热点**
> 相同反馈内容的分析结果缓存 1 小时，避免重复调用。
>
> **5. 模型选择**
> 简单任务用轻量模型（快、便宜），复杂任务用大模型（慢、贵）。"

---

### 10.12 你们怎么处理 LLM 返回结果不稳定的问题？

**回答**：

> "LLM 返回不稳定是常见问题，表现有：
>
> **不稳定表现**
> 1. **格式不稳定**：有时返回 JSON，有时返回文本
> 2. **内容不稳定**：相同输入，不同输出
> 3. **幻觉**：编造不存在的数据或案例
>
> **解决方案**
>
> **1. 结构化输出约束**
> Prompt 中明确指定 JSON 格式，用 Pydantic 校验：
> ```python
> # 解析 LLM 输出
> try:
>     parsed = json.loads(llm_output)
>     result = AnalysisResult(**parsed)
> except (json.JSONDecodeError, ValidationError):
>     # 格式错误时返回默认值
>     result = AnalysisResult(feedback_type="未知")
> ```
>
> **2. Temperature 调低**
> 设置 `temperature=0.1`，让输出更确定性。
>
> **3. 输出校验**
> 检查关键字段是否合理：
> - 温度值在 -50℃~60℃ 范围内
> - 枚举值在预定义列表中
> - 必填字段不为空
>
> **4. 多次采样 + 投票**
> 重要场景调用 3 次，取众数结果。成本高但稳定性好。
>
> **5. Prompt 优化**
> 给更多示例（few-shot），让模型理解期望的输出格式。"

---

### 10.13 你们怎么控制 LLM 调用成本？

**回答**：

> "LLM 调用是按 token 计费的，成本控制很重要：
>
> **成本分布**
> - 输入 token（Prompt + 数据）：占 70%
> - 输出 token（生成结果）：占 30%
>
> **优化方案**
>
> **1. 精简 Prompt**
> 去掉冗余描述，只保留必要指令。从 500 token 优化到 200 token。
>
> **2. 控制输入数据量**
> - 知识库检索结果从 Top-5 降到 Top-3
> - 逐时预报只给最近 6 小时，不给 24 小时
> - 历史案例摘要代替全文
>
> **3. 缓存复用**
> 相同问题的分析结果缓存，避免重复调用。
>
> **4. 模型分级**
> - 简单分类：用轻量模型（便宜 10 倍）
> - 复杂分析：用大模型
>
> **5. 批量处理**
> 多条反馈合并成一个 Prompt（如果场景允许），减少调用次数。
>
> **效果**
> 优化后单次调用成本从 0.05 元降到 0.01 元，下降 80%。"

---

### 10.14 你们遇到过哪些实际开发困难？怎么解决的？

**回答**：

> "几个典型的：
>
> **困难1：中文 Embedding 模型环境配置**
> PyTorch、transformers、sentence-transformers 版本兼容问题多。numpy 2.x 和 torch 2.2 不兼容，需要降级 numpy。
>
> 解决：锁定依赖版本，写 requirements.txt，避免版本漂移。
>
> **困难2：Milvus Collection 状态管理**
> Milvus Lite 的 Collection 查询前需要 load，否则报错 'Collection is in state released'。
>
> 解决：查询前自动 load_collection()，加异常捕获降级到关键词匹配。
>
> **困难3：CSV 数据质量**
> 原始数据有脏数据（severity 字段混入其他值），tags 粒度不统一。
>
> 解决：写数据清洗脚本，统一格式，细分标签。
>
> **困难4：LLM 输出格式不稳定**
> 有时返回 JSON，有时返回文本，解析失败。
>
> 解决：Pydantic 校验 + try-catch 兜底，格式错误时返回默认值。
>
> **困难5：批量分析超时**
> 多条反馈串行调用 LLM，总耗时太长。
>
> 解决：改用 FastAPI BackgroundTasks 异步处理，用户提交后立即返回，后台慢慢跑。"

---

### 10.15 你们怎么监控和优化 Token 使用量？

**回答**：

> "我们从三个维度监控 Token 使用：
>
> **1. 实时监控**
> 每次 LLM 调用记录 input_tokens 和 output_tokens：
> ```python
> response = await client.chat.completions.create(...)
> input_tokens = response.usage.prompt_tokens
> output_tokens = response.usage.completion_tokens
> total_tokens = response.usage.total_tokens
> # 写入日志或监控系统
> ```
>
> **2. 统计分析**
> 按维度统计：
> - 每日/每周/每月总 Token 消耗
> - 单次分析平均 Token 数
> - 各接口 Token 消耗占比
> - 异常高 Token 调用告警
>
> **3. 成本预估**
> 根据单价预估成本：
> - 输入：0.008 元/千 token
> - 输出：0.02 元/千 token
> - 单次分析约 2000 token → 0.02 元
> - 日均 1000 次 → 20 元/天"

---

### 10.16 如何减少 Token 使用量？具体策略有哪些？

**回答**：

> "我们用了 6 种策略：
>
> **策略1：Prompt 精简**
> ```python
> # 优化前：500 token
> prompt = """
> 你是气象数据质量分析专家，负责分析用户反馈的气象数据问题。
> 你的能力包括：1. 分析用户反馈... 2. 检查数据链路... 3. 计算体感温度...
> （冗长描述）
> """
>
> # 优化后：200 token
> prompt = """
> 气象数据分析专家。分析用户反馈，输出 JSON。
> """
> ```
>
> **策略2：数据摘要代替原文**
> ```python
> # 优化前：给完整 24 小时预报（约 500 token）
> hourly_data = [f"{h.time}: {h.temp}℃ 湿度{h.humidity}%" for h in hourly_24h]
>
> # 优化后：只给关键时段（约 100 token）
> hourly_data = [
>     f"最高: {max(h.temp for h in hourly)}℃",
>     f"最低: {min(h.temp for h in hourly)}℃",
>     f"当前: {hourly[0].temp}℃"
> ]
> ```
>
> **策略3：知识库检索精简**
> ```python
> # 优化前：Top-5 全文（约 800 token）
> knowledge = search_knowledge(query, top_k=5)
>
> # 优化后：Top-3 摘要（约 300 token）
> knowledge = search_knowledge(query, top_k=3)
> # 只取 solution 字段，不取全文
> ```
>
> **策略4：结构化输出约束**
> ```python
> # 告诉 LLM 只输出必要字段，不要解释
> prompt += "\n只输出 JSON，不要其他内容。"
> ```
>
> **策略5：缓存复用**
> ```python
> # 相同内容不重复调用
> cache_key = hash(feedback.content)
> if cache_key in cache:
>     return cache[cache_key]
> ```
>
> **策略6：模型分级**
> ```python
> # 简单任务用小模型
> if is_simple_query(feedback):
>     model = "doubao-lite"  # 便宜 10 倍
> else:
>     model = "doubao-pro"
> ```
>
> **效果**
> - 优化前：单次 3000 token，0.05 元
> - 优化后：单次 1200 token，0.01 元
> - 降幅：60%"

---

### 10.17 你们怎么处理 Token 超限问题？

**回答**：

> "Token 超限有几种情况：
>
> **情况1：输入超限**
> 数据太多，超过模型上下文窗口（如 4K/8K/32K）。
>
> 解决：
> - 截断策略：保留最近数据，丢弃旧数据
> - 摘要策略：先用 LLM 总结，再输入
> - 分块策略：分多次调用，最后合并结果
>
> **情况2：输出超限**
> LLM 生成内容太长，被截断。
>
> 解决：
> - Prompt 约束："回答不超过 500 字"
> - max_tokens 参数限制输出长度
> - 流式输出，提前截断
>
> **情况3：对话历史超限**
> 多轮对话累积历史消息。
>
> 解决：
> - 滑动窗口：只保留最近 N 轮
> - 历史摘要：用 LLM 总结历史，替代原文
> - 关键信息提取：只保留实体和结论"

---

### 10.18 你们怎么评估 Prompt 的 Token 效率？

**回答**：

> "我们看两个指标：
>
> **1. 信息密度**
> 有效信息 / 总 Token 数。
> ```python
> # 低密度（信息密度 0.3）
> prompt = "你是气象专家，请仔细分析用户反馈，考虑温度、湿度、风速等因素..."
>
> # 高密度（信息密度 0.8）
> prompt = "分析气象反馈，输出 JSON: {feedback_type, root_cause, reply_content}"
> ```
>
> **2. 输出质量 / Token 成本**
> 相同质量下，Token 越少越好。
> ```python
> # 测试不同 Prompt 版本
> versions = [
>     "详细版（500 token）",
>     "标准版（200 token）",
>     "精简版（100 token）",
> ]
> # 评估每个版本的输出质量
> for v in versions:
>     quality = evaluate_output(v)
>     cost = count_tokens(v)
>     efficiency = quality / cost
> ```
>
> **3. A/B 测试**
> 同一批反馈，用不同 Prompt 测试：
> - 对比输出质量（人工评分）
> - 对比 Token 消耗
> - 选择性价比最高的版本"

---

### 10.19 你们怎么处理 LLM 幻觉问题？

**回答**：

> "LLM 幻觉是指模型编造不存在的事实。我们从预防和检测两方面处理：
>
> **预防措施**
>
> **1. 数据锚定**
> 把真实数据注入 Prompt，让 LLM 基于数据回答：
> ```python
> prompt = f"""
> 基于以下数据回答，不要编造：
> 实况温度：{weather.temp}℃
> 湿度：{weather.humidity}%
> 用户问题：{feedback.content}
> """
> ```
>
> **2. 知识约束**
> 明确告诉 LLM 只用提供的知识：
> ```python
> prompt += """
> 只使用以下知识，不要使用其他知识：
> {knowledge_base}
> """
> ```
>
> **3. 输出校验**
> 检查 LLM 输出是否与输入数据一致：
> ```python
> # 如果 LLM 说"温度 35℃"但实际是 25℃
> if "35℃" in reply and weather.temp != 35:
>     reply = reply.replace("35℃", f"{weather.temp}℃")
> ```
>
> **检测措施**
>
> **1. 事实核查**
> 对比 LLM 输出和原始数据：
> ```python
> def check_hallucination(reply, data):
>     # 提取回复中的数字
>     numbers_in_reply = extract_numbers(reply)
>     # 对比原始数据
>     for num in numbers_in_reply:
>         if num not in data.values():
>             return True  # 可能是幻觉
>     return False
> ```
>
> **2. 多模型交叉验证**
> 用不同 LLM 生成，对比结果：
> ```python
> reply1 = await llm1.generate(prompt)
> reply2 = await llm2.generate(prompt)
> if reply1 != reply2:
>     # 可能有幻觉，人工审核
>     flag_for_review(prompt, reply1, reply2)
> ```
>
> **3. 人工抽检**
> 定期抽查 LLM 输出，标记幻觉案例，优化 Prompt。"

---

### 10.20 你们用的向量数据库为什么会有锁问题？并发性能怎么解决？

**回答**：

> "我们用的是 Milvus Lite，这是个轻量版本，设计上就不支持高并发。
>
> **为什么会有锁问题？**
>
> Milvus Lite 的架构：
> ```
> 进程A ──┐
>         ├──→ [文件锁] ──→ milvus_weather.db
> 进程B ──┘
> ```
>
> 1. **文件级锁**：Milvus Lite 用文件锁防止多进程同时写入，保证数据一致性
> 2. **单进程设计**：它是嵌入式数据库（类似 SQLite），不是客户端-服务器架构
> 3. **gRPC 限制**：内部用 gRPC 通信，连接数有上限，频繁请求会触发 GOAWAY 错误
>
> **具体错误**
> ```
> DataDirLockedError: another process holds the lock
> GOAWAY with error code ENHANCE_YOUR_CALM: too_many_pings
> ```
>
> **解决方案**
>
> **开发阶段（Milvus Lite）**
> ```python
> # 1. 单例模式，只创建一个客户端
> class MilvusClientSingleton:
>     _instance = None
>
>     @classmethod
>     def get_instance(cls):
>         if cls._instance is None:
>             cls._instance = MilvusClient("milvus.db")
>         return cls._instance
>
> # 2. 查询前自动 load
> client.load_collection("weather_feedback")
>
> # 3. 连接池管理
> # 避免频繁创建/销毁连接
> ```
>
> **生产阶段（Milvus Standalone/Distributed）**
> ```python
> # 切换到服务端架构
> client = MilvusClient(uri="http://milvus-server:19530")
>
> # 支持：
> # - 多进程并发访问
> # - 连接池管理
> # - 水平扩展
> # - 高可用
> ```
>
> **版本对比**
>
> | 维度 | Milvus Lite | Milvus Standalone | Milvus Distributed |
> |------|-------------|-------------------|-------------------|
> | 部署 | 嵌入式 | Docker 单机 | K8s 集群 |
> | 并发 | 单进程 | 百级并发 | 万级并发 |
> | 数据量 | <100 万 | <1000 万 | 亿级 |
> | 适用 | 开发测试 | 小规模生产 | 大规模生产 |
> | 锁问题 | 有 | 无 | 无 |
>
> **我们的选择**
> - 开发阶段用 Milvus Lite，快速验证
> - 上线后切 Milvus Standalone，解决并发问题
> - 数据量大了再上 Distributed"
